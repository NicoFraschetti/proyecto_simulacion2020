#ifndef UTILS_HPP
#define UTILS_HPP
    class utils {
        public:
            static double calcularMediaMuestral(double* muestras, int cantidad);
            static double calcularVarianzaMuestral(double* muestras, int cantidad);
};
#endif
